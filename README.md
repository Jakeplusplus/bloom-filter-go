# Go Bloom Filter

A simple bloom filter and CLI written in Go.

## Usage
1. Build the program:
  ```bash
    go build .
  ```

2. Run the program:
  ```bash
    ./bloom-filter-go
  ```

Or just

1. Run the program:
  ```bash
    go run .
  ```
